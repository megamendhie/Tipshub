<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    app:cardCornerRadius="@dimen/activity_horizontal_margin"
    android:padding="@dimen/activity_horizontal_margin"
    android:gravity="center">

  <LinearLayout
      android:layout_width="match_parent"
      android:id="@+id/lnrRoomsDialog"
      android:layout_height="wrap_content"
      android:background="@null"
      android:orientation="vertical"
      android:layout_marginTop="@dimen/activity_horizontal_margin"
      android:layout_marginBottom="@dimen/activity_horizontal_margin"
      android:gravity="center">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:visibility="gone"
        android:text="NOTE: Click WON if only tips won"/>
    <Button
        android:id="@+id/btnSubmit"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:drawableStart="@drawable/ic_check_circle_green_24dp"
        android:text="TIPS WON"/>

    <Button
        android:id="@+id/btnFollow"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:drawableStart="@drawable/ic_follower" />
    <Button
        android:id="@+id/btnRepost"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:drawableStart="@drawable/ic_retweet_color"
        android:text="@string/txt_repost"/>

    <Button
        android:id="@+id/btnDelete"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:drawableStart="@drawable/ic_flag_color_24dp"
        android:text="Flag"/>

  </LinearLayout>
</androidx.cardview.widget.CardView>